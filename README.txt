This folder consists of following files(.ipynb and .py with same functionality). 
	> 10_module_task.py
	> All_Modules.ipynb
	> hcvdat0.csv - Data set

There is slight variation in the arrangement of the code though. 

The variations come in because jupyter is an interactive notebook where we can look at each functionlity in its specific cell. Also, Plots are inline with the execution. Where as,

The "10_module_task.py" file is a generic python code. Therefore, the arrangement of all definitions are on the top and the main function starts after the user defined functions. Also, since, there is no inline feature, plt.show() function was specifically used for each and every plot in an attempt to make environment free usage and reproduction of the results.

